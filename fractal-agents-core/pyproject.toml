[project]
name = "fractal-agents-core"
version = "0.1.0"
description = "Core utilities and shared libraries for FractalForge agents."
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0"
]

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-q"
python_files = "tests/*.py"
